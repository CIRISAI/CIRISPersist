# cargo-deny config — enforces MISSION.md §6 license-locked
# preservation and CRATE_RECOMMENDATIONS §1 license gate.

[graph]
all-features = true

[licenses]
allow = [
    "MIT",
    # v2.5.0 — jsonschema 0.46 (added for #102 Ask 4 envelope-schema
    # validation hook) transitively pulls borrow-or-share@0.2.4 which
    # is MIT-0 (MIT No Attribution). MIT-0 is MIT minus the
    # attribution clause — more permissive, not less, so accepting
    # it alongside MIT is the right call.
    "MIT-0",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MPL-2.0",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "Unlicense",
    "Zlib",
    "OpenSSL",
    "AGPL-3.0",
    "AGPL-3.0-or-later",
    "GPL-3.0",
    "GPL-3.0-or-later",
    "CC0-1.0",
    # v0.1.9 — webpki-roots 0.26+ relicensed to CDLA-Permissive-2.0
    # (Linux Foundation's Community Data License Agreement, permissive
    # variant). It's AGPL-compatible: distribution permitted, no
    # patent-grant requirement, attribution preserved. Transitive
    # via ciris-verify-core's reqwest/ureq stack.
    "CDLA-Permissive-2.0",
]
confidence-threshold = 0.85

[bans]
multiple-versions = "warn"
wildcards = "deny"

[advisories]
yanked = "warn"
# QA-harness findings v0.1.3 → ignore in v0.1.4. Both are
# unmaintained-track advisories pulled in transitively via
# ciris-keyring (CIRISVerify's TPM/keyring stack). Can't fix
# downstream from here. Track upstream for cleanup; the
# unmaintained code paths aren't on our hot path:
# - RUSTSEC-2024-0388 derivative — proc-macro for derive impls
#   (compile-time only, no runtime exposure)
# - RUSTSEC-2024-0384 instant — wall-clock helpers; Phase 2.3
#   Reticulum work may replace this branch entirely
ignore = [
    "RUSTSEC-2024-0388",  # derivative unmaintained (via ciris-keyring TPM derive stack)
    "RUSTSEC-2024-0384",  # instant unmaintained (Phase 2.3 Reticulum may obsolete)

    # v0.1.9 — new advisories surfaced after the v1.6.4 → v1.8.0
    # ciris-keyring/ciris-verify-core bump. All transitive via
    # ciris-verify-core's verification stack (DNS, HTTP, rustls,
    # mobile attestation). Upstream's own deny.toml ignores the
    # rustls-webpki ones with the same rationale; we inherit.
    # None of these advisories touch persist's ingest hot path.
    "RUSTSEC-2026-0119",  # hickory-proto DNS encoding O(n²) — DoS during DNS encoding;
                           # persist talks to Postgres, no DNS encode path
    "RUSTSEC-2026-0118",  # hickory-proto NSEC3 closest-encloser proof loop on cross-zone
                           # responses — a DNSSEC-VALIDATION vuln. Transitive via
                           # ciris-verify-core → hickory-resolver (reqwest's plain resolver);
                           # persist does NOT enable DNSSEC/NSEC3 validation, so the vulnerable
                           # path is unreachable. No patched version exists (patched=[]).
    "RUSTSEC-2025-0134",  # rustls-pemfile unmaintained — transitive via reqwest/rustls
    "RUSTSEC-2026-0098",  # rustls-webpki URI-name constraint (we don't validate URI SANs)
    "RUSTSEC-2026-0099",  # rustls-webpki wildcard-DNS constraint (no name-constraint certs)
    "RUSTSEC-2026-0104",  # rustls-webpki CRL parse panic (we don't parse CRLs)

    # v0.6.0-α4 — restored heavy ML deps (candle / tokenizers / hf-hub
    # / ort / ndarray) for the post-ingest filter pipeline's NER stage.
    # Both transitive advisories are `unmaintained`-track, not
    # exploitable vulnerabilities. Same status as the other
    # unmaintained ignores above. Re-evaluate when candle/ort upgrade.
    "RUSTSEC-2025-0119",  # number_prefix unmaintained — transitive via indicatif → hf-hub
    "RUSTSEC-2024-0436",  # paste unmaintained — transitive via candle / ort macros
    # v6.0.1 — RUSTSEC-2026-0176 + 0177 (pyo3 < 0.29) genuinely CLEARED by
    # the 0.29 re-land; the v5.5.2 ignores are removed, not just unneeded.
    #
    # v11.6.1 — anyhow 1.0.102 `Error::downcast_mut()` Stacked-Borrows
    # unsoundness (dtolnay/anyhow#451, fix pending upstream). anyhow is
    # transitive ONLY via the NER/scrub-ner ML stack (candle / hf-hub);
    # persist never calls `downcast_mut` (the unsound path). Not on the
    # ingest hot path, not exploitable here. Remove when anyhow ships the
    # fix and cargo picks it up. Was blocking pre-push cargo-deny → forcing
    # `--no-verify` bypasses (which let a rustfmt slip reach CI on #323).
    "RUSTSEC-2026-0190",  # anyhow downcast_mut unsound — transitive via candle NER stack
]

[sources]
unknown-registry = "deny"
unknown-git = "warn"
allow-git = []
